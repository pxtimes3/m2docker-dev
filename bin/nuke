#!/usr/bin/env bash

function nuke() {
	docker-compose down --remove-orphans
	docker volume rm $(docker volume ls | awk '{print $2}' | grep -e '^[a-z0-9]')
}

read -p "Do you really want to remove ALL containers & volumes [y/N]? " CHOICE
case "$CHOICE" in 
	y|Y ) nuke;;
	* ) exit;;
esac